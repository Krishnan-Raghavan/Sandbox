# Databases

Organized by engine, since query language and concepts differ a lot between
them. Each engine folder splits `projects/` (standalone work) from
`exercises/` (tutorials, practice queries, course work).

Starter engines: `postgresql/`, `mysql/`, `mongodb/`, `redis/`, `sqlite/`.
Add a new engine folder (same `projects/`/`exercises/` split) as needed.
